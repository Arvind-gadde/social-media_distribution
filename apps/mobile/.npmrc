# Expo + React Native do not understand pnpm symlinked node_modules
# (Metro resolves the same module twice via different paths, e.g.
# react-native's getDevServer ends up as Object via one path and a
# function via the other). Use hoisted layout for this workspace only.
node-linker=hoisted
shamefully-hoist=true
