/**
 * WebSocket Connection Status Indicator
 * 
 * DISABLED: Backend doesn't have WebSocket server yet
 */

'use client';

export function WebSocketStatus() {
  // WebSocket disabled - backend doesn't have WS server
  return null;
}
